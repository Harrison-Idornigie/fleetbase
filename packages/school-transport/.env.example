# School Transport Engine Configuration

# ============================================
# IMPORTANT: CENTRALIZED SETTINGS SYSTEM
# ============================================
# Fleetbase uses centralized settings management.
# Most external services are configured ONCE in the Admin panel:
#
# Console > Admin > System Config > Services
#
# The following services are configured there (NOT in .env):
# - Twilio (SMS notifications)
# - Email/SMTP (email notifications)
# - Google Maps API
# - AWS/Sentry/IPInfo
#
# Settings from Admin panel are stored in the `fleetbase_settings` 
# database table and automatically accessible to all extensions.
#
# ============================================

# Broadcasting Configuration for Real-Time Tracking
# These ARE required in .env (not in Admin panel)
BROADCAST_DRIVER=redis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Queue Configuration (required for broadcasting events)
QUEUE_CONNECTION=redis
# or
# QUEUE_CONNECTION=database

# ============================================
# OPTIONAL: Environment Variables (Advanced)
# ============================================
# Only use these if you prefer .env configuration over Admin panel.
# Admin panel settings override these values.

# Twilio SMS Configuration (OR configure in Admin > System Config > Services)
# TWILIO_ACCOUNT_SID=your_account_sid_here
# TWILIO_AUTH_TOKEN=your_auth_token_here
# TWILIO_FROM_NUMBER=+1234567890

# Email Configuration (OR configure in Admin > System Config)
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USERNAME=null
# MAIL_PASSWORD=null
# MAIL_ENCRYPTION=null
# MAIL_FROM_ADDRESS="noreply@yourdomain.com"
# MAIL_FROM_NAME="${APP_NAME}"

# ============================================
# Module-Specific Settings (stored in config/school-transport.php)
# ============================================
# These settings are specific to School Transport Engine
# and don't conflict with other extensions:

# Route Optimization Settings
# ROUTE_OPTIMIZATION_AVERAGE_SPEED=30  # km/h for urban areas
# ROUTE_OPTIMIZATION_STOP_TIME=2       # minutes per stop for boarding
# ROUTE_OPTIMIZATION_MAX_DURATION=90   # minutes maximum route duration
# ROUTE_OPTIMIZATION_MAX_DISTANCE=50   # km maximum route distance

# Map Tile Configuration
# Default uses free OpenStreetMap tiles - no configuration needed
# MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
# MAP_ATTRIBUTION=© OpenStreetMap contributors
